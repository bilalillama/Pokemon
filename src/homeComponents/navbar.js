import React, { Component } from 'react';

class Navbar extends Component{
  render(){
    return(
      <nav className="navbar bg-dark">
        <a href="#" className="navbar-brand">Pokemon</a>
      </nav>
    );
  }
}

export default Navbar;
